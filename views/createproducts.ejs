<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Product</title>
    <!-- Link Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 text-gray-900 font-sans">
    <%- include('./partials/header') %>

    <% if(success && success.length > 0){ %>
        <div class="absolute top-5 left-1/2 transform -translate-x-1/2 p-3 rounded-md bg-blue-500">
            <span class="inline-block text-white">
                <%= success %>
            </span>
        </div>
    <% } %>

    <div class="min-h-screen flex flex-col">
        <div class="container mx-auto px-10 py-20 flex flex-grow">
            <aside class="w-1/4 h-screen bg-white shadow rounded-lg p-4 mr-6">
                <nav class="flex flex-col space-y-2">
                    <a href="#" class="text-gray-600 hover:text-blue-500 font-medium">All Products</a>
                    <a href="#" class="text-gray-600 hover:text-blue-500 font-medium">Create new product</a>
                </nav>
            </aside>
            <main class="w-3/4 bg-white p-8 shadow rounded-lg">
                <h2 class="text-2xl font-bold mb-4">Create New Product</h2>
                <form autocomplete="off" action="/products/create" method="post" enctype="multipart/form-data">
                    <div class="mb-6">
                        <h3 class="text-xl font-semibold mb-2">Product Details</h3>
                        <div class="mb-4">
                            <label class="block text-gray-700 font-medium mb-2">Product Image</label>
                            <input name="image" type="file" class="py-2 px-4 border border-gray-300 rounded w-full">
                        </div>
                        <div class="grid grid-cols-2 gap-4">
                            <input name="name" type="text" placeholder="Product Name" class="border border-gray-300 p-2 rounded w-full">
                            <input name="price" type="text" placeholder="Product Price" class="border border-gray-300 p-2 rounded w-full">
                            <input name="discount" type="text" placeholder="Discount Price" class="border border-gray-300 p-2 rounded w-full">
                        </div>
                    </div>

                    <div>
                        <h3 class="text-xl font-semibold mb-2">Panel Details</h3>
                        <div class="grid grid-cols-2 gap-4">
                            <input name="bgcolor" type="text" placeholder="Background Color" class="border border-gray-300 p-2 rounded w-full">
                            <input name="panelcolor" type="text" placeholder="Panel Color" class="border border-gray-300 p-2 rounded w-full">
                            <input name="textcolor" type="text" placeholder="Text Color" class="border border-gray-300 p-2 rounded w-full">
                        </div>
                    </div>

                    <button type="submit" class="px-5 py-2 mt-4 bg-blue-500 text-white font-semibold rounded hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-opacity-75">
                        Create New Product
                    </button>
                </form>
            </main>
        </div>
    </div>
    
    <%- include('./partials/footer') %>
</body>

</html>
